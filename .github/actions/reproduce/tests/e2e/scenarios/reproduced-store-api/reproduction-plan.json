{
  "executor": "http",
  "layer": "store-api",
  "issue": 0,
  "version": "e2e",
  "confidence": 1,
  "request": { "method": "GET", "path": "/store-api/context" },
  "assertions": [
    { "kind": "http_status", "op": "equals", "expect": 200, "role": "precondition", "label": "store-api context is reachable" },
    { "field": ".token", "op": "equals", "expect": "this-is-never-the-context-token", "role": "assert", "label": "deliberately-false expectation to drive the reproduced path deterministically" }
  ]
}
