        if (!($#associationClassLc# = $selection->filter('#propertyLc#'))) {
            return;
        }
        $query->leftJoin(
            $selection->getRootEscaped(),
            '#associationTable#',
            $#associationClassLc#->getRootEscaped(),
            sprintf('%s.uuid = %s.#foreignKey#', $#associationClassLc#->getRootEscaped(), $selection->getRootEscaped())
        );
        $this->#associationClassLc#Factory->joinDependencies($#associationClassLc#, $query, $context);
