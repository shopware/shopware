{% block sw_multi_select_option %}
    <li
        class="sw-multi-select-option"
        :class="componentClasses"
        @mouseenter="onMouseEnter($event)"
        @click.stop="onClicked($event)">
        {% block sw_multi_select_option_item_text_holder %}
            <span class="sw-multi-select-option__result-item-text">
                {% block sw_multi_select_option_item_text %}
                    <slot></slot>
                {% endblock %}
            </span>
        {% endblock %}

        {% block sw_multi_select_option_item_icon_transition %}
            <transition name="sw-multi-select-appear">
                {% block sw_multi_select_option_item_icon %}
                    <sw-icon v-if="isSelected(item) && !selected || selected"
                             name="small-default-checkmark-line-medium"
                             size="16px">
                    </sw-icon>
                {% endblock %}
            </transition>
        {% endblock %}
    </li>
{% endblock %}