import 'src/app/state';
import 'src/app/mixin';
import 'module';

/**
 * Wrapper initializers for the module system. We're importing the necessary parts for it in this file.
 * Later on we can use it to inject services, factories, etc. in here
 * @returns {boolean}
 */
export default function createModuleSystem() {
    return true;
}
