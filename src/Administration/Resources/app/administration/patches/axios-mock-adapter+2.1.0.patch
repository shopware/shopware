diff --git a/node_modules/axios-mock-adapter/src/handle_request.js b/node_modules/axios-mock-adapter/src/handle_request.js
index 38945a4..c2a4505 100644
--- a/node_modules/axios-mock-adapter/src/handle_request.js
+++ b/node_modules/axios-mock-adapter/src/handle_request.js
@@ -43,7 +43,7 @@ async function handleRequest(mockAdapter, config) {
     url,
     config.data,
     config.params,
-    (config.headers && config.headers.constructor.name === "AxiosHeaders")
+    (config.headers && config.headers.constructor?.name === "AxiosHeaders")
       ? Object.assign({}, config.headers.toJSON())
       : config.headers,
     config.baseURL
diff --git a/node_modules/axios-mock-adapter/src/utils.js b/node_modules/axios-mock-adapter/src/utils.js
index bcd2945..f761c22 100644
--- a/node_modules/axios-mock-adapter/src/utils.js
+++ b/node_modules/axios-mock-adapter/src/utils.js
@@ -72,7 +72,19 @@ function isObjectMatching(actual, expected) {
   if (typeof expected.asymmetricMatch === "function") {
     return expected.asymmetricMatch(actual);
   }
-  return isEqual(actual, expected);
+  return isEqual(normalizePlainObject(actual), normalizePlainObject(expected));
+}
+
+function normalizePlainObject(value) {
+  if (value === null || typeof value !== "object" || Array.isArray(value)) {
+    return value;
+  }
+
+  if (Object.getPrototypeOf(value) !== null) {
+    return value;
+  }
+
+  return Object.assign({}, value);
 }
 
 function isBodyMatching(body, requiredBody) {
