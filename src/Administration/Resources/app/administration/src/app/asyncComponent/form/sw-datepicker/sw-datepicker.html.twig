{% block sw_datepicker %}
<mt-datepicker
    v-bind="$attrs"
    v-model="realValue"
    :placeholder="placeholder"
>
    <template
        v-for="(index, name) in getSlots()"
        #[name]="data"
    >
        <slot
            :name="name"
            v-bind="data || {}"
        >
        </slot>
    </template>
</mt-datepicker>
{% endblock %}
