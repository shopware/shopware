{# @deprecated tag:v6.8.0 - Will be removed, use mt-card instead. #}
{% block sw_card %}
<mt-card
    v-bind="$attrs"
>
    <template
        v-for="(index, name) in getSlots()"
        #[name]="data"
    >
        <slot
            :name="name"
            v-bind="data"
        >
        </slot>
    </template>
    <slot></slot>
</mt-card>
{% endblock %}
