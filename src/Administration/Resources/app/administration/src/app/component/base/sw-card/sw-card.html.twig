{# v-on="$listeners" needed becaues vue/compat removes them from $attrs #}
{# New mt-card component #}
{% block sw_card %}
<mt-card
    v-if="useMeteorComponent"
    v-bind="$attrs"
>
    <template
        v-for="(index, name) in getSlots()"
        #[name]="data"
    >
        <slot
            :name="name"
            v-bind="data"
        >
        </slot>
    </template>
    <slot></slot>
</mt-card>
{% endblock %}

{# v-on="$listeners" needed becaues vue/compat removes them from $attrs #}
{# Deprecated component #}
{% block sw_card_deprecated %}
<sw-card-deprecated
    v-else
    v-bind="$attrs"
>
    <template
        v-for="(index, name) in getSlots()"
        #[name]="data"
    >
        <slot
            :name="name"
            v-bind="data"
        >
        </slot>
    </template>
    <slot></slot>
</sw-card-deprecated>
{% endblock %}
