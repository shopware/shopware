{# v-on="$listeners" needed becaues vue/compat removes them from $attrs #}
{# New mt-password-field component #}
<mt-password-field
    v-if="useMeteorComponent"
    v-bind="$attrs"
    v-model="realValue"
    :placeholder="placeholder"
>
    <template
        v-for="(index, name) in getSlots()"
        #[name]="data"
    >
        <slot
            :name="name"
            v-bind="data || {}"
        >
        </slot>
    </template>
</mt-password-field>

{# v-on="$listeners" needed becaues vue/compat removes them from $attrs #}
{# Deprecated component #}
<sw-password-field-deprecated
    v-else
    v-bind="$attrs"
    v-model:value="realValue"
    :placeholder="placeholder"
    v-on="listeners"
>
    <template
        v-for="(index, name) in getSlots()"
        #[name]="data"
    >
        <slot
            :name="name"
            v-bind="data || {}"
        >
        </slot>
    </template>
</sw-password-field-deprecated>
