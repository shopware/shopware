# Styling & Design Tokens (Skeleton)

- Token tiers: primitive (color, spacing, typography), semantic tokens
- Storage format & source of truth (placeholder)
- Consumption in Vue components (SCSS vars / CSS vars)
- Dark mode / theme switching placeholder
- Responsive breakpoints naming
- Icon system linkage
- Guidelines: avoid hard-coded values; reference tokens
- Linting / style checks (placeholder)
