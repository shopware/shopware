import template from './sw-cms-block-sidebar-filter.html.twig';

const { Component } = Shopware;

/**
 * @private since v6.5.0
 */
Component.register('sw-cms-block-sidebar-filter', {
    template,
});
