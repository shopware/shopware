{% block sw_experience_studio_list %}
<sw-page class="sw-experience-studio-list">
    {% block sw_experience_studio_list_search_bar %}
    <template #search-bar>
        <sw-search-bar
            initial-search-type="content_layout"
            :initial-search="term"
            @search="onSearch"
        />
    </template>
    {% endblock %}

    {% block sw_experience_studio_list_smart_bar %}
    <template #smart-bar-header>
        <h2>{{ $t('sw-experience-studio.general.moduleTitle') }}</h2>
    </template>

    <template #smart-bar-actions>
        <mt-button
            v-tooltip="{
                message: $t('sw-privileges.tooltip.warning'),
                disabled: allowCreate,
                showOnDisabledElements: true
            }"
            variant="primary"
            :disabled="!allowCreate || undefined"
            @click="onCreateNewLayout"
        >
            {{ $t('sw-experience-studio.general.createNewLayout') }}
        </mt-button>
    </template>
    {% endblock %}

    {% block sw_experience_studio_list_content %}
    <template #content>
        <div class="sw-experience-studio-list__content">
            <sw-entity-listing
                v-if="layouts && layouts.length > 0"
                :repository="layoutRepository"
                detail-route="sw.experience.studio.detail"
                :columns="columnConfig"
                :data-source="layouts"
                :is-loading="isLoading"
                :show-selection="false"
                :allow-inline-edit="false"
                :allow-column-edit="false"
                :sort-by="sortBy"
                :sort-direction="sortDirection"
                @page-change="onPageChange"
                @column-sort="onSortColumn"
            >
                <template #more-actions="{ item }">
                    <sw-context-menu-item
                        class="sw-experience-studio-list__show-drafts-action"
                        @click="onShowDrafts(item)"
                    >
                        {{ $t('sw-experience-studio.list.actionShowDrafts') }}
                    </sw-context-menu-item>
                </template>
            </sw-entity-listing>

            <mt-empty-state
                v-else-if="!isLoading"
                icon="regular-palette"
                :headline="$t('sw-experience-studio.list.emptyStateTitle')"
                :description="$t('sw-experience-studio.list.emptyStateSubline')"
            />

            <sw-loader v-else />

            <sw-experience-studio-drafts-modal
                v-if="draftsModalLayout"
                :layout-id="draftsModalLayout.id"
                :layout-name="draftsModalLayout.name"
                @modal-close="onCloseDraftsModal"
            />
        </div>
    </template>
    {% endblock %}
</sw-page>
{% endblock %}
