{% block sw_order_product_select %}
    <span class="sw-order-product-select">
        {% block sw_order_product_select_blank_item%}
            <sw-text-field v-if="isShownItemLabelInput"
                      v-model="item.label"
                      size="small"
                      :placeholder="$tc('sw-order.detailBase.columnProductName')">
            </sw-text-field>
        {% endblock %}

        {% block sw_order_product_select_existing_product %}
            <sw-entity-single-select class="sw-order-product-select__single-select"
                                     v-else-if="isShownProductSelect"
                                     :context="contextWithInheritance"
                                     :criteria="productCriteria"
                                     v-model="product"
                                     entity="product"
                                     @change="onItemChanged"
                                     size="small">
                <template v-if="next7399" #selection-label-property="{ item }">
                    <slot name="selection-label-property" v-bind="{ item }">
                        <div>{{ item.name || item.translated.name }}</div>

                        <sw-product-variation
                            v-if="item.variation"
                            :variations="item.variation">
                        </sw-product-variation>
                    </slot>
                </template>

                <template v-if="next7399" #result-item="{ item, index }">
                    <slot name="result-item" v-bind="{ item, index }">
                        <li is="sw-select-result" v-bind="{ item, index }">
                            {% block sw_entity_single_select_base_results_list_result_label %}
                                <span class="sw-select-result__result-item-text">
                                    {{ item.translated.name || item.name }}
                                </span>

                                <sw-product-variation
                                    v-if="item.variation"
                                    :variations="item.variation">
                                </sw-product-variation>
                            {% endblock %}
                        </li>
                    </slot>
                </template>
            </sw-entity-single-select>
        {% endblock %}

        {% block sw_order_product_show_product %}
            <template v-else>
                {{ item.label }}
            </template>
        {% endblock %}
    </span>
{% endblock %}
