{% block sw_sales_channel_menu %}
<div class="sw-sales-channel-menu">
    {% block sw_sales_channel_menu_modal %}
    <sw-sales-channel-modal
        v-if="showModal"
        @modal-close="showModal=false"
    />
    {% endblock %}

    {% block sw_sales_channel_menu_headline %}
    <div class="sw-admin-menu__headline">
        {% block sw_sales_channel_menu_headline_text %}
        <div class="collapsible-text sw-admin-menu__headline_text hide-on-collapse">
            <router-link
                :to="{ name: 'sw.sales.channel.list' }"
            >{{ $t('sw-sales-channel.general.titleMenuItems') }}</router-link>
        </div>
        {% endblock %}

        {% block sw_sales_channel_menu_headline_context_button %}
        <mt-dropdown-menu-root
            :open="contextMenuOpen"
            @update:open="contextMenuOpen = $event"
        >
            <mt-dropdown-menu-trigger as-child>
                <mt-button
                    class="sw-admin-menu__headline-action"
                    variant="tertiary"
                    size="x-small"
                    square
                    :aria-label="$t('sw-sales-channel.general.manageSalesChannels')"
                >
                    <template #iconFront>
                        <mt-icon
                            name="solid-ellipsis-h-s"
                            size="14px"
                        />
                    </template>
                </mt-button>
            </mt-dropdown-menu-trigger>

            <mt-dropdown-menu-portal>
                <mt-action-menu
                    :side="isSidebarExpanded ? 'bottom' : 'right'"
                    :side-offset="isSidebarExpanded ? 4 : 12"
                >
                    {% block sw_sales_channel_menu_headline_context_button_item %}
                    <router-link
                        v-slot="{ navigate }"
                        :to="{ name: 'sw.sales.channel.list' }"
                        custom
                    >
                        <mt-action-menu-item
                            class="sw-admin-menu__headline-context-menu-manage-sales-channels"
                            icon="regular-pencil-s"
                            @click="navigate"
                        >
                            {{ $t('sw-sales-channel.general.manageSalesChannels') }}
                        </mt-action-menu-item>
                    </router-link>
                    {% endblock %}

                    {% block sw_sales_channel_menu_headline_action %}
                    <mt-action-menu-item
                        v-if="canCreateSalesChannels"
                        class="sw-admin-menu__headline-context-menu-add-sales-channel"
                        icon="regular-plus-circle"
                        @click="openSalesChannelModal"
                    >
                        {{ $t('sw-sales-channel.general.addSalesChannel') }}
                    </mt-action-menu-item>
                    {% endblock %}
                </mt-action-menu>
            </mt-dropdown-menu-portal>
        </mt-dropdown-menu-root>
        {% endblock %}
    </div>
    {% endblock %}

    {% block sw_sales_channel_menu_navigation %}
    <nav class="sw-admin-menu__navigation">

        {% block sw_sales_channel_menu_navigation_main_list %}
        <ul class="sw-admin-menu__navigation-list">

            {% block sw_sales_channel_menu_navigation_main_items %}
            <sw-admin-menu-item
                v-for="(entry, index) in buildMenuTree"
                :key="entry.id || index"
                class="sw-admin-menu__sales-channel-item"
                :entry="entry"
                :sidebar-expanded="isSidebarExpanded"
                icon-size="16px"
                :class="['sw-admin-menu__sales-channel-item--' + index]"
            >
                <template #additional-text>
                    {% block sw_sales_channel_menu_navigation_item_additional_text %}
                    <mt-button
                        v-if="entry.domainLink && entry.active"
                        class="sw-sales-channel-menu-domain-link"
                        variant="tertiary"
                        size="x-small"
                        square
                        :aria-label="$t('sw-sales-channel.general.tooltipOpenStorefront')"
                        :title="$t('sw-sales-channel.general.tooltipOpenStorefront')"
                        @click.prevent="openStorefrontLink(entry.domainLink)"
                    >
                        <template #iconFront>
                            <mt-icon
                                class="sw-sales-channel-menu-domain-link__icon hide-on-collapse"
                                name="regular-eye"
                                size="16px"
                            />
                        </template>
                    </mt-button>
                    {% endblock %}
                </template>
            </sw-admin-menu-item>
            {% endblock %}

            {% block sw_sales_channel_menu_navigation_more_items %}
            <sw-admin-menu-item
                v-if="moreSalesChannelAvailable"
                :entry="moreItemsEntry"
                :sidebar-expanded="isSidebarExpanded"
                :show-active-state="false"
                class="sw-admin-menu__sales-channel-more-items"
                icon-size="16px"
            />
            {% endblock %}

            {% block sw_sales_channel_menu_navigation_add_channel %}
            <li
                v-if="showAddChannelMenuItem"
                class="sw-admin-menu__navigation-list-item sw-sales-channel-menu__add-channel"
            >
                <div class="sw-admin-menu__navigation-item-row">
                    <button
                        type="button"
                        class="sw-admin-menu__navigation-link"
                        :aria-label="$t('sw-sales-channel.general.addSalesChannel')"
                        @click="openSalesChannelModal"
                    >
                        <mt-icon
                            name="regular-plus-circle"
                            size="16px"
                            class="sw-admin-menu__navigation-link-icon"
                        />
                        <span class="sw-admin-menu__navigation-link-label collapsible-text hide-on-collapse">
                            {{ $t('sw-sales-channel.general.addSalesChannel') }}
                        </span>
                    </button>
                </div>
            </li>
            {% endblock %}
        </ul>
        {% endblock %}
    </nav>
    {% endblock %}
</div>
{% endblock %}
