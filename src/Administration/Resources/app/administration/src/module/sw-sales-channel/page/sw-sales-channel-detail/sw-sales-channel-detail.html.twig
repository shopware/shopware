{% block sw_sales_channel_detail %}
<sw-page class="sw-sales-channel-detail">
    {% block sw_sales_channel_detail_header %}
    <template #smart-bar-header>
        {% block sw_sales_channel_detail_header_headline %}
        <h2>{{ placeholder(salesChannel, 'name', $t('sw-sales-channel.detail.textHeadline')) }}</h2>
        {% endblock %}
    </template>
    {% endblock %}

    {% block sw_sales_channel_detail_actions %}
    <template #smart-bar-actions>
        {% block sw_sales_channel_detail_actions_save %}
        <sw-button-process
            v-tooltip.bottom="tooltipSave"
            class="sw-sales-channel-detail__save-action"
            :is-loading="isLoading"
            :process-success="isSaveSuccessful"
            :disabled="!allowSaving || isLoading || productComparison.invalidFileName"
            variant="primary"
            data-analytics-id="sw_sales_channel.detail.sales_channel_save"
            :data-analytics-sales-channel-type-id="salesChannel?.typeId ?? ''"
            :data-analytics-sales-channel-is-new="salesChannel?.isNew?.() ?? false"
            @update:process-success="saveFinish"
            @click.prevent="onSave"
        >
            {{ $t('sw-sales-channel.detail.buttonSave') }}
        </sw-button-process>
        {% endblock %}
    </template>
    {% endblock %}

    {% block sw_sales_channel_detail_language_switch %}
    <template #language-switch>
        <sw-language-switch
            :save-changes-function="saveOnLanguageChange"
            :abort-change-function="abortOnLanguageChange"
            @on-change="onChangeLanguage"
        />
    </template>
    {% endblock %}

    {% block sw_sales_channel_detail_content %}
    <template #content>
        <sw-card-view>

            {% block sw_sales_channel_detail_content_language_info %}
            <sw-language-info
                :entity-description="placeholder(salesChannel, 'name', $t('sw-sales-channel.detail.textHeadline'))"
            />
            {% endblock %}

            {% block sw_sales_channel_detail_content_tabs %}
            <sw-tabs
                class="sw-sales-channel-detail-__tabs"
                position-identifier="sw-sales-channel-detail"
            >
                {%  block sw_sales_channel_detail_content_tab_general %}
                <sw-tabs-item
                    :route="{ name: 'sw.sales.channel.detail.base', params: { id: $route.params.id } }"
                    :title="$t('sw-sales-channel.detail.tabBase')"
                >
                    {{ $t('sw-sales-channel.detail.tabBase') }}
                </sw-tabs-item>
                {% endblock %}

                {% block sw_sales_channel_detail_content_tab_product_export_insights %}
                <sw-tabs-item
                    v-if="isAgenticCommerce && !isLoading"
                    :route="{ name: 'sw.sales.channel.detail.productExportInsights', params: { id: $route.params.id } }"
                    :title="$t('sw-sales-channel.detail.productExport.tabInsights')"
                >
                    {{ $t('sw-sales-channel.detail.productExport.tabInsights') }}
                </sw-tabs-item>
                {% endblock %}

                {% block sw_sales_channel_detail_content_tab_products %}
                <sw-tabs-item
                    v-if="isHeadless || isStorefront"
                    :route="{ name: 'sw.sales.channel.detail.products', params: { id: $route.params.id } }"
                    :title="$t('sw-sales-channel.detail.tabProducts')"
                >
                    {{ $t('sw-sales-channel.detail.tabProducts') }}
                </sw-tabs-item>
                {% endblock %}

                {% block sw_sales_channel_detail_content_tab_theme %}{% endblock %}

                {% block sw_sales_channel_detail_content_tab_agentic_commerce_integration %}
                <sw-tabs-item
                    v-if="isAgenticCommerce && !isLoading"
                    :route="{ name: 'sw.sales.channel.detail.agenticCommerceIntegration', params: { id: $route.params.id } }"
                    :title="$t('sw-sales-channel.detail.agenticCommerce.tabIntegration')"
                >
                    {{ $t('sw-sales-channel.detail.agenticCommerce.tabIntegration') }}
                </sw-tabs-item>
                {% endblock %}

                {% block sw_sales_channel_detail_content_tab_export_template %}
                {# @deprecated tag:v6.8.0 - Block will be removed. Use `sw_sales_channel_detail_content_tab_export_template` instead. #}
                {%  block sw_sales_channel_detail_content_tab_product_comparison %}
                <sw-tabs-item
                    v-if="isProductExportChannel && !isLoading"
                    :route="{ name: 'sw.sales.channel.detail.productComparison', params: { id: $route.params.id } }"
                    :title="$t('sw-sales-channel.detail.tabProductComparison')"
                >
                    {{ $t('sw-sales-channel.detail.tabProductComparison') }}
                </sw-tabs-item>
                {% endblock %}
                {% endblock %}

                {%  block sw_sales_channel_detail_content_tab_analytics %}
                <sw-tabs-item
                    v-if="isStorefront"
                    :route="{ name: 'sw.sales.channel.detail.analytics', params: { id: $route.params.id } }"
                    :title="$t('sw-sales-channel.detail.tabAnalytics')"
                >
                    {{ $t('sw-sales-channel.detail.tabAnalytics') }}
                </sw-tabs-item>
                {% endblock %}

                <sw-tabs-item
                    v-if="isHeadless || isStorefront"
                    :route="{ name: 'sw.sales.channel.detail.agenticFiles', params: { id: $route.params.id } }"
                    :title="$t('sw-sales-channel.detail.tabAgenticFiles')"
                >
                    {{ $t('sw-sales-channel.detail.tabAgenticFiles') }}
                </sw-tabs-item>
            </sw-tabs>
            {% endblock %}

            {% block sw_sales_channel_detail_content_view %}
            <template v-if="isLoading">
                <sw-skeleton />
                <sw-skeleton />
            </template>

            <template v-else>
                <router-view
                    :key="$route.params.id"
                    v-slot="{ Component }"
                >
                    <component
                        :is="Component"
                        :sales-channel="salesChannel"
                        :product-export="productExport"
                        :agentic-commerce-export-config="agenticCommerceExportConfig"
                        :storefront-sales-channel-criteria="storefrontSalesChannelCriteria"
                        :custom-field-sets="customFieldSets"
                        :is-loading="isLoading"
                        :product-comparison-access-url="productComparison.productComparisonAccessUrl"
                        :template-options="productComparison.templateOptions"
                        :show-template-modal="productComparison.showTemplateModal"
                        :template-name="productComparison.templateName"
                        @template-selected="onTemplateSelected"
                        @access-key-changed="generateAccessUrl"
                        @domain-changed="generateAccessUrl"
                        @invalid-file-name="setInvalidFileName(true)"
                        @valid-file-name="setInvalidFileName(false)"
                        @template-modal-close="onTemplateModalClose"
                        @template-modal-confirm="onTemplateModalConfirm"
                    />
                </router-view>
            </template>
            {% endblock %}
        </sw-card-view>
    </template>
    {% endblock %}

    <template #sidebar>
        {% block sw_sales_channel_detail_sidebar %}
        {% endblock %}
    </template>
</sw-page>
{% endblock %}
