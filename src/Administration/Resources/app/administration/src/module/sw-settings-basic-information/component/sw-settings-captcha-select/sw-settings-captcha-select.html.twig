{# @deprecated tag:v6.5.0 - The block content will be deleted and switch to use sw-settings-captcha-v2 #}
{% block sw_settings_captcha_select %}
    <div v-if="!feature.isActive('FEATURE_NEXT_12455')"
        class="sw-settings-captcha-select">
        <sw-multi-select
                :options="captchas"
                ref="component"
                v-model="currentValue"
                v-bind="attributes">
        </sw-multi-select>
    </div>
{% endblock %}
