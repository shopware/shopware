{% block sw_highlighted_text %}
<div class=".sw-highlighted-text">
    <span
        v-for="(part, index) in styledLabels"
        :key="JSON.stringify(part) + index"
        :class="{'sw-highlighted-text--highlight': part.highlight}"
    >
        {{ part.text }}
    </span>
</div>
{% endblock %}
