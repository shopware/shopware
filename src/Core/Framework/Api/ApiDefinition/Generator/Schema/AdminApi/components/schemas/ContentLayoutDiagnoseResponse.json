{
    "openapi": "3.1.0",
    "info": [],
    "paths": [],
    "components": {
        "schemas": {
            "ContentLayoutDiagnoseResponse": {
                "title": "Content layout diagnose response",
                "type": "object",
                "description": "The response returned by the resolve-and-diagnose route: the per-element property resolutions plus the well-formedness / resolvability report for the supplied draft layout. Never persisted, never cached.",
                "required": ["resolutions", "diagnostics", "applicableBindings"],
                "properties": {
                    "resolutions": {
                        "allOf": [
                            { "$ref": "#/components/schemas/ContentLayoutResolutions" }
                        ],
                        "description": "Per-element property resolutions for the whole tree. Encodes as `{}` when empty.",
                        "readOnly": true
                    },
                    "diagnostics": {
                        "allOf": [
                            { "$ref": "#/components/schemas/ContentLayoutDiagnosticsReport" }
                        ],
                        "description": "The well-formedness / resolvability report. With no source bound (neither `entityType` nor `section`), only intrinsic well-formedness is evaluated and `resolvable` is not meaningful.",
                        "readOnly": true
                    },
                    "applicableBindings": {
                        "type": "object",
                        "description": "Map of element id to the source-qualified binding specification ids (`source:id`) applicable to that element's type, keyed by declared type rather than by the element's actual wiring. Pass an id back as `bindingSpecificationId` to a bind-element action. An entry is emitted for every element in the tree; a listed element's own list encodes as `[]` when its type has no applicable specification. The map itself encodes as `{}` only when the tree has no elements.",
                        "additionalProperties": {
                            "type": "array",
                            "items": { "type": "string" }
                        },
                        "readOnly": true,
                        "example": { "0188fa1b2c3d4e5f6a7b8c9d0e1f2a40": ["core:from-media-library"] }
                    }
                },
                "example": {
                    "resolutions": {
                        "0188fa1b2c3d4e5f6a7b8c9d0e1f2a40": [
                            {
                                "key": "media",
                                "kind": "reference",
                                "required": false,
                                "type": null,
                                "default": null,
                                "fqcn": "Shopware\\Core\\Content\\Media\\MediaEntity",
                                "resolved": null,
                                "candidates": []
                            }
                        ]
                    },
                    "diagnostics": {
                        "wellFormed": true,
                        "resolvable": true,
                        "violations": [
                            {
                                "code": "unresolved_optional",
                                "scope": "binding",
                                "severity": "warning",
                                "elementId": "0188fa1b2c3d4e5f6a7b8c9d0e1f2a40",
                                "key": "media",
                                "message": "Optional property \"media\" has no source.",
                                "candidates": []
                            }
                        ]
                    },
                    "applicableBindings": {
                        "0188fa1b2c3d4e5f6a7b8c9d0e1f2a40": ["core:from-media-library"]
                    }
                }
            }
        }
    }
}
