{
  "openapi": "3.2.0",
  "info": [],
  "paths": {
    "/cookie-groups": {
      "get": {
        "tags": [
          "Experimental",
          "Cookies"
        ],
        "summary": "Fetch all cookie groups",
        "description": "Fetch all cookie groups available in the system with configuration hash. Experimental API, not part of our backwards compatibility promise, thus this API can introduce breaking changes at any time.",
        "operationId": "readCookieGroups",
        "responses": {
          "200": {
            "description": "Successful response with a collection of cookie groups and their configuration hash.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CookieRouteResponse"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/400"
          },
          "401": {
            "$ref": "#/components/responses/401",
            "description": "The `sw-access-key` header is missing."
          }
        },
        "security": [
          {
            "ApiKey": []
          }
        ],
        "parameters": [
          {
            "$ref": "#/components/parameters/swLanguageId"
          }
        ]
      }
    },
    "/cookie-consent-log": {
      "post": {
        "tags": [
          "Experimental",
          "Cookies"
        ],
        "summary": "Log a cookie consent decision",
        "description": "Records a visitor's cookie consent decision together with a snapshot of the current cookie configuration, so shop operators can demonstrate that consent was obtained (GDPR Art. 7(1)). The per-group verdict (`accepted`, `partial`, `rejected`) is derived server-side from the reported cookie names. The record is pseudonymous: apart from the client-generated `consentId` no visitor identifier is stored. Experimental API, not part of our backwards compatibility promise, thus this API can introduce breaking changes at any time.",
        "operationId": "logCookieConsent",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": [
                  "consentId",
                  "consentAction"
                ],
                "properties": {
                  "consentId": {
                    "type": "string",
                    "pattern": "^[A-Za-z0-9_-]{1,64}$",
                    "description": "Opaque token the client generates once (e.g. a UUID) and keeps in its own cookie storage, so it can be reused for later decisions of the same visitor. It links a visitor's decisions, so the records of one data subject can be filtered out of an export. Must not contain personal data."
                  },
                  "consentAction": {
                    "type": "string",
                    "enum": [
                      "accept_all",
                      "accept_required",
                      "accept_selected"
                    ],
                    "description": "Which consent action the visitor performed."
                  },
                  "acceptedCookies": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Names of the cookies the visitor accepted. Only evaluated for `accept_selected`; the other actions are fully determined by the action itself. Cookie names unknown to the current configuration are ignored. An empty or absent list means nothing beyond the technically required cookies was accepted."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "The consent decision was logged."
          },
          "400": {
            "$ref": "#/components/responses/400"
          },
          "429": {
            "$ref": "#/components/responses/429",
            "description": "Too many consent decisions were logged for this client IP. The number of seconds to wait before retrying is contained in the error."
          }
        },
        "security": [
          {
            "ApiKey": []
          }
        ],
        "parameters": [
          {
            "$ref": "#/components/parameters/swLanguageId"
          }
        ]
      }
    }
  }
}
