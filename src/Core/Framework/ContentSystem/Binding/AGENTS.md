> Conceptual overview and design rationale live in [README.md](README.md), same
> directory. The references and constraints below cover most code changes; read
> the README when you need the mental model.

## Navigation

- Specification model — [docs/specification-model.md](docs/specification-model.md)
- Authoring tiers — [docs/authoring-sugar.md](docs/authoring-sugar.md)
- Entity-name derivation — [docs/entity-name-derivation.md](docs/entity-name-derivation.md)
- `resolvedBy` shorthand — [docs/resolved-by.md](docs/resolved-by.md)
- Inline `bindings:`, app overlay — [docs/inline-bindings.md](docs/inline-bindings.md)
- The per-type default — [docs/default-specification.md](docs/default-specification.md)
- Load-time validation — [docs/validation.md](docs/validation.md)
- Loading, registry, apps — [docs/loading-and-apps.md](docs/loading-and-apps.md)
- Applying a binding — [docs/applying.md](docs/applying.md)
- Write-boundary attribution — [docs/write-boundary.md](docs/write-boundary.md)
- Introspection fold — [docs/introspection.md](docs/introspection.md)
- Authoring guide — [docs/custom-specifications.md](docs/custom-specifications.md)

## Source Code References

- **Value objects** (`Specification/`): `BindingSpecification` (`id`, `type`, `label`, `resolves`, `inputs`, `source`; `qualifiedId()` → `source:id`, `isDefault()`, `toSchema()`), `LoaderBinding` (`source`, `config`), `BindingInput` (`hasDefault (bool)`, `default (mixed)`, `required (bool)` — derived by the canonicalizer)
- **resolvedBy loader branch**: `ResolvedByLoaderBranch` (module root; `@internal` enum, cases `Entity`/`EntityCollection`) — the closed tier-A classification: `fromReferenceFqcn(fqcn)` (`null` for neither), `loaderSource()` → the built-in loader's `SOURCE` constant (`EntityLoader::SOURCE` / `EntityCollectionLoader::SOURCE`, never a bare string literal), `fromLoaderSource(source)` (inverse), `matchesStoredValueShape(value)` (per-branch stored-value shape check)
- **Default synthesizer**: `DefaultBindingSpecificationSynthesizer` (module root; `@internal`) — injected into `YamlBindingSpecificationLoader`. `synthesize(data, type, path): ?array` (`null` when the file declares no `resolvedBy` property). Enforces `MAX_ID_LENGTH = 255` on the minted id (the type name) and the storage-key collision rules as hard load errors naming the file
- **Registry**: `Registry/AbstractContentSystemBindingSpecificationRegistry` (decoration pattern; `all(): array<string, BindingSpecification>` keyed by `source:id`, `byType(type): list<BindingSpecification>`, `get(qualifiedId): ?BindingSpecification`, `invalidate()`), `Registry/ContentSystemBindingSpecificationRegistry` (leaf aggregator over the tagged loaders), `Registry/CachedContentSystemBindingSpecificationRegistry` (`cache.system` decorator, cache key `content_system.binding_specifications`)
- **Compiler Pass**: `Framework/DependencyInjection/CompilerPass/ContentSystemElementTypeCompilerPass` — injects the discovered element-type directories into `YamlBindingSpecificationLoader` as well as `Layout/Type/Loader/YamlTypeLoader`. Any new throw uses `DependencyInjectionException` (that namespace must not throw `ContentSystemException`)
- **Loaders**: `Loader/AbstractContentSystemBindingSpecificationLoader` (`load(): list<BindingSpecification>`), `Loader/YamlBindingSpecificationLoader` (filesystem; one load method, `loadDtosFromTypeDirectory(directory, source, prefix, typeOverlay = [])`), `Loader/DatabaseBindingSpecificationLoader` (active app bindings in prod, empty in dev), `Layout/Type/Loader/ElementTypeSourceDirectory` (`source`, `path`, `prefix`; shared with the type loader), `Loader/ResolvedBindingSpecificationDto` (`id`, `source`, `dto`; `toSpecification()`)
- **Serializer**: `Serialization/BindingSpecificationSerializer` (`denormalize` array → DTO; `normalize` DTO → array, used to hash/persist an app binding's schema)
- **Canonicalizer**: `Serialization/BindingSpecificationCanonicalizer` — load-time expansion of sugared `resolves` entries into canonical `{loader, config}` form. `canonicalize(dto, id, typeOverlay = [])` resolves the declared type **overlay-first, then registry**. Unknown type → `bindingSpecificationUnknownType` (400); non-deterministic sugar → `bindingSpecificationCanonicalizationFailed` (400); neither is a `CLIENT_DEFECT_CODE`
- **Specification DTO**: `Specification/Dto/BindingSpecificationDto` (`#[WellFormedBindingSpecification]`; carries no id — the loader passes the `bindings:` map key to `toBindingSpecification(id, source)`), `Specification/Dto/BindingSpecificationDtoCollection` (`#[TypeConsistentBindingSpecification]` at the class level, plus a `typeOverlay` field the semantic validator reads)
- **Validators**: `Validation/WellFormedBindingSpecification` (+ validator) — class-level on the DTO, structural shape only, no registry lookups; `Validation/TypeConsistentBindingSpecification` (+ validator) — **class-level on `BindingSpecificationDtoCollection`**; violation paths are keyed on the binding id (`bindings[<id>].…`)
- **Applicator**: `BindingApplicator` (final instance service, injects `Hydration/DataLoader/DataLoaderConfigSerializerProvider`) — `apply(element, specification, bindingSpecificationId): StoredElement` (overwrite) and `applyFillOnly()` (fill-only, same signature); both rebuild the element rather than mutating it
- **Mutation op**: `Mutation/Op/BindElement` (extends `Mutation/AbstractLayoutMutation`) — `apply()` looks up the specification (`bindingSpecificationNotFound`), locates the target node (`mutationTargetNotFound`), checks `specification->type() === node->getComponent()` (`bindingTypeMismatch`); all three are `400`s. `Mutation/Op/InsertElement` and `Mutation/Op/ReplaceElement` fill-apply the type's default (`AbstractLayoutMutation::resolveDefaultSpecification()`); `InsertElement` also applies a request's explicit `bindingSpecificationId` on top, with the same lookup and type check
- **AttributionReconciler**: `AttributionReconciler` (write seam, the last of `Layout/LayoutWriteBoundary`'s three passes, after `Layout/LayoutDefaultSeeder` and style normalization) — re-derives every element's `attributedSpecifications` on every DAL write of the `layout` field; a diverged or dangling attribution is dropped, never an error
- **API endpoints**: `Api/LayoutMutationController::bind()`/`insert()` — `POST /api/_action/content-system/layout/{bind-element,insert-element}`, names `api.action.content_system.layout.{bind_element,insert_element}`; `Api/ContentLayoutMutationController::bind()`/`insert()` — the same two paths under `/layout/{layoutId}/`, names `api.action.content_system.layout.{persisted_bind_element,persisted_insert_element}`. Both `insert()` actions apply an optional request `bindingSpecificationId` onto the freshly scaffolded element
- **App integration**: `App/Aggregate/AppContentSystemBindingSpecification/AppContentSystemBindingSpecificationDefinition` (DAL entity, table `app_content_system_binding_specification`, `UNIQUE KEY` on `(app_id, name)`), `App/Lifecycle/Persister/ContentSystemBindingSpecificationPersister`, `App/Lifecycle/Handler/ContentSystemBindingSpecificationLifecycleHandler`, `App/Validation/ContentSystemBindingSpecificationAppValidator` (constructor is the binding loader plus the type loader only — no registry dependency), `App/Validation/Error/ContentSystemBindingSpecificationSchemaError`
- **Migration**: `Migration/V6_7/Migration1782423128AddAppContentSystemBindingSpecificationTable` — creates `app_content_system_binding_specification` (`id`, `app_id` FK `ON DELETE CASCADE`, `name`, `schema` JSON, `hash`, timestamps)
- **Diagnostics tie-ins** (fed by stored wiring here): `Resolution/CandidateOrigin::Stored`, `Diagnostics/ViolationCode::MismatchedReferenceType` — see [Resolution](../Resolution/AGENTS.md), [Diagnostics](../Diagnostics/AGENTS.md)

## Constraints

- Uniqueness is by source-qualified id (`"source:id"`), not by bare id: `ContentSystemBindingSpecificationRegistry::all()` keys on `BindingSpecification::qualifiedId()`, so only a duplicate **within** one source/directory throws `bindingSpecificationDuplicate`
- **App type overlay:** the app persister and validator pass a type overlay built from the app's own `Resources/content-system/types`; every non-app path passes an empty overlay — `YamlBindingSpecificationLoader::load()` (the compiler-pass scan) and `DatabaseBindingSpecificationLoader` rows
- **Default uniqueness** is at most one default specification per element type (`isDefault()` derives `id === type`, computed, never stored). An authored `bindings:` key equal to the file's type name is rejected before duplicate detection (`bindingSpecificationReservedId`, 409). At *application* time the default set for a type is read zero/one/more: no-op / fill-applied / `bindingSpecificationDefaultAmbiguous` (409)
- `TypeConsistentBindingSpecification` resolves each `resolves` entry's produced type via `Diagnostics/RootContextMapper::resolveType()`; a client-defect exception becomes a validation violation, any other propagates
- Loaders are tagged `content_system.binding_specification_loader`. Core ships no dedicated binding-specification directory and no authored inline `bindings:` entry: every core binding specification is a synthesized default, six in all, each from the `resolvedBy` properties of one file under `Layout/Type/Definitions/` — `core:Sw:Media:Image` (`media` from the `mediaId` storage key, `media/image.yaml`), `core:Sw:Grid:Container` (`backgroundImage` from `backgroundImageId`, `grid/container.yaml`), `core:Sw:Media:Youtube` (`previewMedia` from `previewMediaId`, `media/youtube.yaml`) and `core:Sw:Media:Vimeo` (`previewMedia` from `previewMediaId`, `media/vimeo.yaml`), all four wired via the `entity` loader; `core:Sw:Media:Gallery` (`mediaItems` from `mediaIds`, `media/gallery.yaml`) via the `entity_collection` loader, its property being a `MediaCollection` rather than a `MediaEntity`; and `core:Sw:Navigation:Tree` (`navigationTree`, `navigation/tree.yaml`), whose `resolvedBy` is a tier-B loader block (`navigation: {rootId: main-navigation}`) rather than a bare storage key — it wires the `navigation` loader and names no storage key at all
- `DatabaseBindingSpecificationLoader` validates each row independently (identified by `source:id`); a row whose schema fails to decode or validate is skipped and logged at `warning` level rather than failing the whole load — unlike `YamlBindingSpecificationLoader`, which fails hard on an authored file
