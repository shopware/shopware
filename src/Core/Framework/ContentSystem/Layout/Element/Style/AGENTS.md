> Conceptual overview and design rationale live in [README.md](README.md), same
> directory. The references and constraints below cover most code changes; read
> the README when you need the mental model.

## Key Relationship

One declaration, two consumers that cannot drift:
- `StyleOptionConstraintDeriver` turns a `StyleOptionValueType` into the Symfony constraints the write boundary enforces.
- `StyleOptionSpecification::toSchema()` turns the same declaration into the introspection schema.
- Both read the one `AbstractContentSystemStyleOptionRegistry`, so an option that is introspected is enforced and vice versa.

## Source Code References

- **Registry**: `Registry/AbstractContentSystemStyleOptionRegistry` (abstract, decoration pattern; strict `all()` / lenient `allResolved()`, both `array<string, StyleOptionSpecification>`, plus `invalidate()`), `Registry/ContentSystemStyleOptionRegistry` (stateless aggregator over loaders tagged `content_system.style_option_loader`), `Registry/CachedContentSystemStyleOptionRegistry` (`cache.system` pool decorator, cache keys `content_system.style_options` and `content_system.style_options.resolved`, both cleared on `invalidate()`)
- **Compiler Pass**: `Framework/DependencyInjection/CompilerPass/ContentSystemStyleOptionCompilerPass` (discovers core `Definitions/`, each bundle's and active plugin's fixed `Resources/content-system/style-options`, and dev-only active app directories; injects them into `YamlStyleOptionLoader`)
- **Loaders**: `Loader/AbstractContentSystemStyleOptionLoader` (base contract, `load(): list<StyleOptionSpecification>`), `Loader/YamlStyleOptionLoader` (filesystem; `loadDtosFromDirectory($directory, $source)` is reused by the app persister), `Loader/DatabaseStyleOptionLoader` (active app options in prod, empty in dev), `Loader/StyleOptionSourceDirectory` (`source`, `path` VO), `Loader/ResolvedStyleOptionSpecificationDto` (`name`, `source`, `dto`; `toSpecification()`)
- **Serializer**: `Serialization/StyleOptionSpecificationSerializer` (`denormalize` array → DTO, `normalize` DTO → array; `normalize` emits `breakpointAware` and `kind` only when `!== null`, like `enum` / `range` / `maxLength` / `default` — so an absent flag is omitted but an explicit `false` is kept on round-trip; this is the shape the app persister stores into `app_content_system_style_option.schema`, so `kind` survives install and reload)
- **Specification**: `Specification/StyleOptionSpecification` (const `KIND_BOX_SPACING`; `name()`, `valueType()`, `breakpointAware()`, `source()`, `kind()`, `toSchema()`; `toSchema()` always emits `breakpointAware` before `adminUI`, and does **not** carry `kind` — the introspection shape is unchanged), `Specification/StyleOptionValueType` (consts `TYPE_STRING|INTEGER|NUMBER|BOOLEAN`, `PRIMITIVE_TYPES`, `DEFAULT_STRING_MAX_LENGTH = 255`; `type()`, `enum()`, `range()`, `maxLength()`, `default()`, `isPrimitive()`, `toSchema()`), DTOs in `Specification/Dto/`
- **Constraint deriver**: `Validation/StyleOptionConstraintDeriver::derive(StyleOptionValueType): list<Constraint>` — emits via the fluent `DataAbstractionLayer/Write/Validation/ConstraintBuilder`; applies `NotBlank` to every type **except** `boolean` (false is a valid value), plus `Range` / `Length` / `Choice` from the declared bounds
- **Write constraints**: `Layout/Codec/StoredTreeConstraints::styleConstraints()` (the write boundary; branches on `breakpointAware()` per option — a breakpoint-aware option gets a per-breakpoint `Collection`, a flat one a single `Optional`. Both levels compose `NotNull` ahead of the derived constraints, because `Type` skips a null and a boolean option carries no `NotBlank` — without it the descriptor would admit a null value `decodeStyle()` refuses), reached from `Layout/Field/StoredElementListFieldSerializer::buildConstraints()`
- **Read**: `Layout/Codec/StoredElementCodec::decodeStyle()` is registry-free and structural — a scalar is kept flat, an array must be a non-empty map keyed by `Breakpoint::values()` with scalar values, and unknown options ride through verbatim. Every malformed shape throws (`invalidMapKey` for a non-string option name, `unknownStyleBreakpoint` for an unknown breakpoint key, `invalidFieldValueType` for a value that is neither scalar nor array, a non-scalar breakpoint value, and an explicitly empty breakpoint map); nothing is dropped. Strictness is structural, not registry-driven
- **API endpoints**: `Api/Controller/InfoController::getContentSystemStyleOptions()` (`GET /api/_info/content-system-style-options.json`) and the folded `styleOptions` key on `getContentSystemElementTypes()`
- **App integration**: `App/Aggregate/AppContentSystemStyleOption/AppContentSystemStyleOptionDefinition` (DAL entity, table `app_content_system_style_option`, `UNIQUE KEY` on `name`), `App/Lifecycle/Persister/ContentSystemStyleOptionPersister` (const `STYLE_OPTIONS_DIRECTORY = 'Resources/content-system/style-options'`; hash-based upsert/delete in one transaction, registry invalidation only after a committed change), `App/Lifecycle/Handler/ContentSystemStyleOptionLifecycleHandler` (persists on install/update; invalidates the registry on activate/deactivate/uninstall/local delete — activation does not pre-validate for a collision, since it cannot fail atomically after `active=1` is committed; `all()` surfaces a cross-loader collision loudly on the next write/install instead), `App/Validation/ContentSystemStyleOptionAppValidator`
- **Collision detection**: `Validation/StyleOptionCollisionDetector` (proposed names against the registry plus inactive app options; the DB `UNIQUE KEY` is the authoritative guard)
- **Value object**: `ElementStyle` (`array<string, string|int|float|bool|array<string, string|int|float|bool>>` of `option => (scalar | breakpoint => scalar)`; `final readonly`, `toArray()`, `isEmpty()`)

## Constraints

- Option names are flat global wire keys (`col-span`), **not** source-prefixed like element type names. The name is the kebab-case filename (`YamlStyleOptionLoader::NAME_PATTERN` = `[a-z][a-z0-9]*(-[a-z0-9]+)*`, leading letter required so a name can never coerce to an int array key on read). Uniqueness is enforced across all sources by `StyleOptionCollisionDetector` and the `app_content_system_style_option.name` `UNIQUE KEY`. The write and install paths fail hard on a cross-loader duplicate (`all()` throws `styleOptionDuplicate` with source labels `core` / `bundle:Name` / `plugin:Name` / `app:Name`); the introspection endpoints use the lenient `allResolved()`, which resolves a duplicate silently by source precedence (core, bundle, plugin, database/app; first-registered wins within a tier) — `all()` is the loud surface, so a collision is never logged from inside the cached read view. The content decode/serve path is registry-free, so a collision never reaches it.
- One option per YAML file. The file is a **flat** declaration (`type`, optional `enum` / `range` / `maxLength` / `default`, optional `breakpointAware`, optional `adminUI`, optional `kind`) — there is **no** `meta:` wrapper, unlike element-type YAML. `breakpointAware` defaults to `true` (`buildBreakpointAware()`: absent ⇒ `true`); an option opts out with `breakpointAware: false`. No shipped core option declares it — the flag is forward-looking, and every shipped core option stays breakpoint-aware. `kind` is the option's write-time canonicalisation discriminator: its only defined value is `StyleOptionSpecification::KIND_BOX_SPACING` (`box-spacing`), which makes `ElementStyleNormalizer` canonicalise the value into explicit four-part CSS; any other value fails the declaration's `Assert\Choice`, so a bad `kind` fails hard at `YamlStyleOptionLoader` and is skipped-with-a-warning at `DatabaseStyleOptionLoader`. No shipped core option declares it either.
- `Breakpoint` is a fixed framework primitive (`xs, sm, md, lg, xl, xxl`); it is not plugin- or app-extensible. `Breakpoint::values()` is the allowed key set; per-breakpoint values are individually optional.
- Value types are limited to the canonical primitives (`string`, `integer`, `number`, `boolean`). No FQCN, no nested object, no regex. A string or number with no declared `maxLength` is bounded by `DEFAULT_STRING_MAX_LENGTH = 255` (surfaced by `maxLength()` and the introspection schema) so a client cannot store an unbounded value (including a long numeric string) in the JSON column.
- `default` is an authoring hint (introspection/Admin pre-fill), never applied at serve time, so `style` stays omitted-when-empty — an option whose every value equals its default drops as unset. It reaches stored JSON only through `ElementStyleNormalizer` (run at the write boundary by `Layout/LayoutWriteBoundary`), which fills the *missing* breakpoints of a partially specified map from the default. Storage already held such values, expanded Admin-side before sending; only the seeding site moved to the server.
- Write is strict over what `ElementStyleNormalizer` leaves, since it runs first: a breakpoint-aware option sent as a bare scalar is broadcast into the full breakpoint map instead of rejected. An unknown breakpoint key never reaches the normalizer at all: `StoredElementCodec::decodeStyle()` runs first on every request and column payload and rejects it, so the normalizer's own key-dropping in `expandBreakpointMap()` applies only to an `ElementStyle` built in-process. The canonical form that survives is then judged per flag — unknown option, unknown breakpoint, constraint-violating value and a flat option sent as a breakpoint map are all rejected. Read (`StoredElementCodec::decodeStyle()`) stays registry-free, so a stored row naming an option the registry no longer knows still renders — but it is no longer structurally lenient, so a row carrying a malformed style shape (reachable only by a raw-SQL or migration write, or by a DAL write predating the codec) now throws on read rather than being cleaned. Re-saving a layout whose `style` still references a removed option is rejected until it is cleared, so a normal edit round-trip no longer auto-clears it — the same posture the element type system has for an unknown `component`. `Diagnostics/ViolationCode::UnknownStyleOption` (intrinsic, error) names one where the write only rejects: the diagnose and draft mutation routes carry the code in their 200 diagnostics; the preview route 400s with the message, not the code; the DAL write — hence the persisted mutation routes, which commit before diagnosing — rejects at this serializer's `encode()` constraint, before the diagnostics gate runs.
- The constraint `Collection` is derived fresh per write and reused across every element within that write, not memoized across writes.
- Registry uses the Shopware decoration pattern (abstract → leaf → `cache.system` decorator, above). Consumers type-hint the abstract; `invalidate()` throws `DecorationPatternException` except on the cached decorator.
- `DatabaseStyleOptionLoader` returns empty in dev, where apps load from the filesystem via the compiler pass. It joins `app` and filters `WHERE app.active = 1`, so a deactivated app's options drop from that query with no extra write, and the lifecycle handler's invalidations (above) make the cached set reflect an activation change immediately rather than lagging until the next install/update; `StyleOptionCollisionDetector` also considers inactive app options to prevent cross-app name collisions. A persisted row whose schema fails to decode or validate is skipped and logged at `warning` level rather than failing the whole load — unlike `YamlStyleOptionLoader`, which fails hard on an authored file.
- The `Plugin` base class is untouched: bundles and plugins share the fixed `Resources/content-system/style-options` convention directory (an optional `Plugin::getStyleOptionDirectory()` hook was deliberately deferred).
- `ElementStyle` immutability is load-bearing: the mutation subsystem aliases an untouched element's `ElementStyle` by reference into rebuilt and cloned nodes, which is only safe because it cannot be changed in place.
