shopware:
    filesystem:
        private:
            type: "memory"
            config:
                root: ~
        public:
            type: "memory"
            config:
                root: ~

    admin_worker:
        poll_interval: 1

    media:
        enable_url_upload_feature: true
        enable_url_validation: true
