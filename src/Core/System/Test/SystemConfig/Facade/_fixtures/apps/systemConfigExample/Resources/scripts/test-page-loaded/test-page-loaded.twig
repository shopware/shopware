{% set page = hook.page %}
{# @var page \Shopware\Storefront\Page\Page #}

{% set systemConfig = services.config.get('general.system_config') %}
{% set appConfig = services.config.app('app_config') %}

{% do page.addArrayExtension('systemConfigExtension', {
    'systemConfig': systemConfig,
    'appConfig': appConfig,
}) %}
