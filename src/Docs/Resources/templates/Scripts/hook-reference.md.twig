<!--- auto generated by `bin/console docs:generate-scripting-reference` in the shopware project, don't edit this file manually -->
# List of all available Hooks for Scripting

{% for useCase in data %}
## {{ useCase.title }}

{{ useCase.description }}

{% for hook in useCase.hooks %}
#### {{ hook.name }}

| <!-- -->               | <!-- -->                                                                                                                                                             |
|:-----------------------|:---------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Name**               | {{ hook.name }}                                                                                                                                                      |
| **Class**              | `{{ hook.class }}`                                                                                                                                                   |
| **Description**        | {{ hook.trigger|raw }}                                                                                                                                               |
| **Available Data**     | {% for data in hook.data %}{{ data.name }}: {% if data.link is not null %}[`{{ data.type }}`]({{ data.link }}){% else %}`{{ data.type }}`{% endif %} <br>{% endfor%} |
| **Available Services** | {% for service in hook.services %}[{{ service.name }}]({{ service.link }})<br>{% endfor%}                                                                            |

{% endfor %}

{% endfor %}
