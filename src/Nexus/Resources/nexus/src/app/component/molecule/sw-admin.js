import ComponentFactory from 'src/core/factory/component.factory';
import template from 'src/app/component/molecule/sw-admin/sw-admin.html.twig';

export default ComponentFactory.register('sw-admin', {
    template
});
