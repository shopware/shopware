{
  "name": "storefront-admin-extension",
  "version": "1.0.0",
  "description": "Adding the storefront related changes to the administration.",
  "main": "./src/main.js",
  "config": {
    "admin_path": "../../../../Administration/Resources/app/administration"
  },
  "scripts": {
    "format": "$npm_package_config_admin_path/node_modules/.bin/prettier --check \"src/**/*.{js,ts}\"",
    "format:fix": "$npm_package_config_admin_path/node_modules/.bin/prettier --write \"src/**/*.{js,ts}\" --cache",
    "lint": "$npm_package_config_admin_path/node_modules/.bin/eslint src --cache",
    "lint:fix": "npm run lint -- --fix"
  }
}
