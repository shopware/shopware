/**
 * @private
 * @sw-package discovery
 *
 * Components from the Storefront's admin-modules that need to be wrapped
 * synchronously in tests (e.g. when the test relies on refs or sync mounting).
 */
export default [];
