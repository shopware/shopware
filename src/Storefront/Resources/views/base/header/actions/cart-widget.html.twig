{% block base_header_actions_cart_widget %}
    {# @TODO: NEXT-1981 - apply CSS styling to this widget #}
    <a class="btn btn-light cart-widget cart-widget-btn" href="{{ path('frontend.checkout.cart.page') }}"
       data-cart-widget="true"
       data-cart-mini=true
       title="{{ 'checkout.cartTitle'|trans }}">
        <span class="cart-widget-display">{{ 'checkout.cartTitle'|trans }}</span>
        <span class="cart-widget-badge">{{ page.cart.lineItems|length }}</span>
        <i class="icon-basket"></i>
        <span class="cart-widget-total">
            {{ page.cart.price.positionPrice|localizedcurrency(page.context.currency.shortName, app.request.locale) }}
        </span>
    </a>
{% endblock %}
