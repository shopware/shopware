{% block component_review_point %}
    {% if size is not defined %}
        {% set size = 'xs' %}
    {% endif %}

    <span class="product-review-point">
        {% if type == 'half' %}
            {% sw_icon 'star-half' style { 'color': 'review', 'size': size } %}
        {% elseif type == 'blank' %}
            {% sw_icon 'star' style { 'color': 'light', 'size': size} %}
        {% else %}
            {% sw_icon 'star' style { 'color': 'review', 'size': size} %}
        {% endif %}
    </span>
{% endblock %}
