{% props
    header
%}

{% set attributeDefaults = {
    class: 'sw-top-bar',
} %}

<div {{ attributes.defaults(attributeDefaults) }}>
    <div class="sw-top-bar__container container d-flex justify-content-end">
        {% block content %}{% endblock %}
    </div>
</div>
