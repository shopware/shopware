{% props
    header
%}

{% set attributeDefaults = {
    class: 'sw-top-bar',
} %}

<div {{ attributes.defaults(attributeDefaults) }}>
    <div class="sw-top-bar__container container d-flex justify-content-end">

        <twig:Sw:StoreTemplate:LanguageSelect :languages="header.languages" />
        <twig:Sw:StoreTemplate:CurrencySelect :currencies="header.currencies" />

    </div>
</div>
