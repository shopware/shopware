{% sw_extends '@Storefront/frontend/checkout/cart/cart-item.html.twig' %}

{% block frontend_checkout_cart_item_details_title %}
    {% set detailUrl = path('detail_page', { 'id': lineItem.key }) %}
    {% if lineItem.extensions.canonicalUrl %}
        {% set detailUrl = lineItem.extensions.canonicalUrl.url %}
    {% endif %}

    <a class="item--link"
       href="{{ detailUrl }}"
       title="{{ lineItem.label|truncate(160) }}">
        <span class="item--quantity">{{ lineItem.quantity }}x</span>
        {{ parent() }}
    </a>
{% endblock %}

{% block frontend_checkout_cart_item_details_sku %}{% endblock %}
{% block frontend_checkout_cart_item_quantity %}{% endblock %}
{% block frontend_checkout_cart_item_price %}{% endblock %}

{% block frontnend_checkout_cart_item_delete_form %}
    <div class="action--container">
        {% set deleteUrl = path('cart_delete_line_item') %}

        <a href="{{ deleteUrl }}"
           class="btn is--small action--remove"
           data-identifier="{{ lineItem.key }}"
           title="{% trans %}frontend.checkout.ajax_cart.AjaxCartRemoveArticle{% endtrans %}">
            <i class="icon--cross"></i>
        </a>
    </div>
{% endblock %}
