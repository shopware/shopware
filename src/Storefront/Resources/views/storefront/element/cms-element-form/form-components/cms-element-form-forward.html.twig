{% block cms_form_element_forward %}
    {% if forwardTo %}
        <input type="hidden" name="forwardTo" value="{{ forwardTo }}"/>

        {% if forwardParams %}
            <input type="hidden" name="forwardParameters" value="{{ forwardParams }}"/>
        {% endif %}
    {% endif %}
{% endblock %}
