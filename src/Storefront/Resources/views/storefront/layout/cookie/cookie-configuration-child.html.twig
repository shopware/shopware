{% if child.cookie is not empty and child.hidden is not same as(true) %}
    <div class="offcanvas-cookie-entry custom-control custom-checkbox">
        <input
            type="checkbox"
            class="custom-control-input"
            id="cookie_{{ child.cookie }}"
            {% if isRequired %}
                checked="checked"
                disabled="disabled"
                data-cookie-required="true"
            {% endif %}
            {% if child.cookie %}
                data-cookie="{{ child.cookie }}"
            {% endif %}
            {% if child.value %}
                data-cookie-value="{{ child.value }}"
            {% endif %}
            {% if child.expiration %}
                data-cookie-expiration="{{ child.expiration }}"
            {% endif %}>
        <label
            for="cookie_{{ child.cookie }}"
            class="custom-control-label">
            {{ child.snippet_name ? child.snippet_name|trans|sw_sanitize : child.cookie }}
        </label>


        {% if child.snippet_description %}
            <p>{{ child.snippet_description|trans|sw_sanitize }}</p>
        {% endif %}
    </div>
{% endif %}
