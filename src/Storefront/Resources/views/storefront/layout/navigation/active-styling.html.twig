{# @deprecated tag:v6.8.0 - File will be removed. The active styling class `.active` is set by navbar.plugin.js. #}

{# Styles the current active navigation and the parent navigations #}

{# The style tag is different for each navigation and will be cached accordingly #}

{# @deprecated tag:v6.8.0 - Block "layout_navigation_active_styling" will be removed because the file is deprecated. The active styling class `.active` is set by navbar.plugin.js. #}
{% block layout_navigation_active_styling %}
    {# @deprecated tag:v6.8.0 - Variable `navigationId` will be removed because the file is deprecated. The active styling class `.active` is set by navbar.plugin.js. #}
    {% set navigationId = shopware.navigation.id %}
{% endblock %}
