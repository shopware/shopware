{% do services.storefront.ensureCustomerIsLoggedIn(hook.query['allow-guest']|boolval) %}

{% do hook.setResponse(
    services.storefront.render('@MyApp/storefront/page/customer-greeting/index.html.twig', { 'page': hook.page, 'customer': hook.salesChannelContext.customer })
) %}
