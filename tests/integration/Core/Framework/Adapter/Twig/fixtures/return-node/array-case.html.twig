{% apply spaceless %}
    {% macro foo(index) %}
        {% set arr = [1, 2] %}
        {{ arr[index] }}
    {% endmacro %}

    {% if _self.foo(1) is empty %}
        should not be empty
    {% endif %}
    {{ _self.foo(1) }}
{% endapply %}
